## 0.1.0

- Agrega `FailureType` (network, server, unknown) a `Failure` como parámetro opcional.
- Agrega adaptadores `asJsonMap` y `asJsonList` para castear respuestas JSON de forma segura.

## 0.0.1

- Versión inicial del paquete: `Result`, `Success`, `Failure`, `DioGetClient`.